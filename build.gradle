plugins {
  id 'com.ullink.msbuild' version '3.15'
}

msbuild {
    solutionFile = 'MathQuizBot.sln'
}

task testH(type: Copy, dependsOn: [msbuild]) {
    from 'MathQuizBot/bin/Debug/net6.0'
    into 'heroku_output'
    include '**'
}